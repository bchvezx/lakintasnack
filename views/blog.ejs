<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog - <%= config.name || 'La Quinta Snack Bar' %></title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        :root {
            --primary-color: #e74c3c;
            --secondary-color: #2c3e50;
            --dark-bg: #2c3e50;
        }

        .navbar-brand {
            font-weight: bold;
            color: var(--primary-color) !important;
        }

        .btn-primary {
            background-color: var(--primary-color);
            border-color: var(--primary-color);
        }

        .btn-primary:hover {
            background-color: #c0392b;
            border-color: #c0392b;
        }

        .blog-card {
            transition: transform 0.3s ease;
        }

        .blog-card:hover {
            transform: translateY(-5px);
        }

        footer {
            background-color: var(--dark-bg);
            color: white;
            padding: 3rem 0 1rem;
        }

        .social-links a {
            color: white;
            font-size: 1.5rem;
            margin: 0 0.5rem;
            transition: color 0.3s ease;
        }

        .social-links a:hover {
            color: var(--primary-color);
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light sticky-top">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-utensils"></i> <%= config.name || 'La Quinta Snack Bar' %>
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">Inicio</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/#menu">Menú</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="/blog">Blog</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/#contact">Contacto</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/admin" style="color: var(--primary-color);">
                            <i class="fas fa-cog"></i> Admin
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container py-5">
        <div class="text-center mb-5">
            <h1 class="display-4 fw-bold">Blog y Noticias</h1>
            <p class="lead">Mantente al día con nuestras últimas novedades, promociones y eventos especiales</p>
        </div>

        <% if (posts && posts.length > 0) { %>
        <div class="row">
            <% posts.forEach(post => { %>
            <div class="col-lg-6 mb-4">
                <div class="card blog-card h-100 shadow-sm">
                    <% if (post.image) { %>
                    <img src="/uploads/<%= post.image %>" class="card-img-top" alt="<%= post.title %>" style="height: 250px; object-fit: cover;">
                    <% } %>
                    <div class="card-body d-flex flex-column">
                        <h4 class="card-title text-primary"><%= post.title %></h4>
                        <p class="card-text flex-grow-1">
                            <%= post.content.substring(0, 200) %>...
                        </p>
                        <div class="mt-auto">
                            <div class="d-flex justify-content-between align-items-center">
                                <small class="text-muted">
                                    <i class="fas fa-calendar me-1"></i>
                                    <%= new Date(post.created_at).toLocaleDateString('es-ES', { 
                                        year: 'numeric', 
                                        month: 'long', 
                                        day: 'numeric' 
                                    }) %>
                                </small>
                                <a href="/blog/<%= post.id %>" class="btn btn-primary">
                                    Leer más <i class="fas fa-arrow-right ms-1"></i>
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <% }); %>
        </div>
        <% } else { %>
        <div class="text-center py-5">
            <i class="fas fa-blog fa-4x text-muted mb-4"></i>
            <h3 class="text-muted">No hay publicaciones disponibles</h3>
            <p class="text-muted">Pronto tendremos noticias y novedades para compartir contigo.</p>
            <a href="/" class="btn btn-primary mt-3">
                <i class="fas fa-home me-2"></i>Volver al inicio
            </a>
        </div>
        <% } %>
    </div>

    <!-- Footer -->
    <footer>
        <div class="container">
            <div class="row">
                <div class="col-md-4">
                    <h5><i class="fas fa-utensils"></i> <%= config.name || 'La Quinta Snack Bar' %></h5>
                    <p>Donde cada bocado es una experiencia única.</p>
                </div>
                <div class="col-md-4">
                    <h5>Contacto</h5>
                    <p><i class="fas fa-phone"></i> <%= config.phone || '' %></p>
                    <p><i class="fas fa-envelope"></i> <%= config.email || '' %></p>
                    <p><i class="fas fa-map-marker-alt"></i> <%= config.address || '' %></p>
                </div>
                <div class="col-md-4">
                    <h5>Síguenos</h5>
                    <div class="social-links">
                        <% if (config.facebook) { %>
                        <a href="<%= config.facebook %>" target="_blank"><i class="fab fa-facebook"></i></a>
                        <% } %>
                        <% if (config.instagram) { %>
                        <a href="<%= config.instagram %>" target="_blank"><i class="fab fa-instagram"></i></a>
                        <% } %>
                        <% if (config.twitter) { %>
                        <a href="<%= config.twitter %>" target="_blank"><i class="fab fa-twitter"></i></a>
                        <% } %>
                        <% if (config.whatsapp) { %>
                        <a href="https://wa.me/<%= config.whatsapp.replace(/[^0-9]/g, '') %>" target="_blank"><i class="fab fa-whatsapp"></i></a>
                        <% } %>
                    </div>
                </div>
            </div>
            <hr class="my-4">
            <div class="text-center">
                <p>&copy; 2024 <%= config.name || 'La Quinta Snack Bar' %>. Todos los derechos reservados.</p>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>