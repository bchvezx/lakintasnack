<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>La Quinta Snack Bar - Comida Fresca y Deliciosa</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        :root {
            --primary-color: #e74c3c;
            --secondary-color: #2c3e50;
            --dark-bg: #2c3e50;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
        }

        .navbar-brand {
            font-weight: bold;
            color: var(--primary-color) !important;
        }

        .btn-primary {
            background-color: var(--primary-color);
            border-color: var(--primary-color);
        }

        .btn-primary:hover {
            background-color: #c0392b;
            border-color: #c0392b;
        }

        .hero-section {
            background: linear-gradient(rgba(44, 62, 80, 0.8), rgba(231, 76, 60, 0.8)), 
                        url('https://images.adsttc.com/media/images/5259/dbbd/e8e4/4e67/bf00/08fc/large_jpg/Portada.jpg?1381620645') center/cover;
            min-height: 60vh;
            display: flex;
            align-items: center;
            color: white;
        }

        .card {
            border: none;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease;
        }

        .card:hover {
            transform: translateY(-5px);
        }

        .whatsapp-float {
            position: fixed;
            width: 60px;
            height: 60px;
            bottom: 40px;
            right: 40px;
            background-color: #25d366;
            color: #FFF;
            border-radius: 50px;
            text-align: center;
            font-size: 30px;
            box-shadow: 2px 2px 3px #999;
            z-index: 100;
            display: flex;
            align-items: center;
            justify-content: center;
            text-decoration: none;
            animation: pulse 2s infinite;
        }

        .whatsapp-float:hover {
            color: #FFF;
            background-color: #128c7e;
        }

        @keyframes pulse {
            0% { box-shadow: 0 0 0 0 rgba(37, 211, 102, 0.7); }
            70% { box-shadow: 0 0 0 10px rgba(37, 211, 102, 0); }
            100% { box-shadow: 0 0 0 0 rgba(37, 211, 102, 0); }
        }

        .menu-item {
            border-bottom: 1px solid #eee;
            padding: 1rem 0;
        }

        .menu-item:last-child {
            border-bottom: none;
        }

        .price {
            color: var(--primary-color);
            font-weight: bold;
            font-size: 1.1rem;
        }

        footer {
            background-color: var(--dark-bg);
            color: white;
            padding: 3rem 0 1rem;
        }

        .social-links a {
            color: white;
            font-size: 1.5rem;
            margin: 0 0.5rem;
            transition: color 0.3s ease;
        }

        .social-links a:hover {
            color: var(--primary-color);
        }

        @media (max-width: 768px) {
            .whatsapp-float {
                width: 50px;
                height: 50px;
                bottom: 20px;
                right: 20px;
                font-size: 25px;
            }
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light sticky-top">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-utensils"></i> <%= config.name || 'La Quinta Snack Bar' %>
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">Inicio</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#menu">Menú</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/blog">Blog</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#contact">Contacto</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/admin" style="color: var(--primary-color);">
                            <i class="fas fa-cog"></i> Admin
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="hero-section">
        <div class="container">
            <div class="row">
                <div class="col-lg-8">
                    <h1 class="display-4 fw-bold mb-4"><%= config.name || 'La Quinta Snack Bar' %></h1>
                    <p class="lead mb-4">Donde cada bocado es una experiencia única. Comida fresca, sabores auténticos y un ambiente acogedor te esperan.</p>
                    <a href="#menu" class="btn btn-primary btn-lg me-3">Ver Menú</a>
                    <% if (config.whatsapp) { %>
                    <a href="https://wa.me/<%= config.whatsapp.replace(/[^0-9]/g, '') %>?text=Hola, quiero hacer un pedido" 
                       class="btn btn-success btn-lg" target="_blank">
                        <i class="fab fa-whatsapp"></i> Pedir Ahora
                    </a>
                    <% } %>
                </div>
            </div>
        </div>
    </section>

    <!-- About Section -->
    <section class="py-5">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-lg-6">
                    <h2 class="mb-4">Sobre Nosotros</h2>
                    <p class="lead">En <%= config.name || 'La Quinta Snack Bar' %>, nos apasiona servir comida fresca y deliciosa en un ambiente familiar y acogedor.</p>
                    <p>Utilizamos ingredientes frescos y locales para preparar cada plato con amor y dedicación. Nuestro equipo trabaja día a día para ofrecerte la mejor experiencia gastronómica.</p>
                    <div class="row mt-4">
                        <div class="col-sm-6">
                            <div class="d-flex align-items-center mb-3">
                                <i class="fas fa-clock text-primary me-3 fs-4"></i>
                                <div>
                                    <h6 class="mb-0">Horarios</h6>
                                    <small class="text-muted"><%- config.hours || 'Consultar horarios' %></small>
                                </div>
                            </div>
                        </div>
                        <div class="col-sm-6">
                            <div class="d-flex align-items-center mb-3">
                                <i class="fas fa-phone text-primary me-3 fs-4"></i>
                                <div>
                                    <h6 class="mb-0">Teléfono</h6>
                                    <small class="text-muted"><%= config.phone || 'Contactar' %></small>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="col-lg-6">
                    <img src="https://laguiadelasvitaminas.com/wp-content/uploads/2018/08/sandwich-de-tomate-espinaca-y-queso-a-la-parrilla.jpg" 
                         alt="Comida fresca" class="img-fluid rounded shadow">
                </div>
            </div>
        </div>
    </section>

    <!-- Menu Section -->
    <section id="menu" class="py-5 bg-light">
        <div class="container">
            <div class="text-center mb-5">
                <h2 class="display-5 fw-bold">Nuestro Menú</h2>
                <p class="lead">Descubre nuestros deliciosos platos preparados con ingredientes frescos</p>
            </div>

            <% 
            const categories = [...new Set(menuItems.map(item => item.category))];
            categories.forEach(category => { 
            %>
            <div class="row mb-5">
                <div class="col-12">
                    <div class="card">
                        <div class="card-header bg-primary text-white">
                            <h4 class="mb-0">
                                <% if (category === 'Comidas Principales') { %>
                                    <i class="fas fa-hamburger me-2"></i>
                                <% } else if (category === 'Bebidas') { %>
                                    <i class="fas fa-coffee me-2"></i>
                                <% } else { %>
                                    <i class="fas fa-cookie-bite me-2"></i>
                                <% } %>
                                <%= category %>
                            </h4>
                        </div>
                        <div class="card-body">
                            <% menuItems.filter(item => item.category === category).forEach(item => { %>
                            <div class="menu-item">
                                <div class="row align-items-center">
                                    <div class="col-md-9">
                                        <h6 class="mb-1"><%= item.name %></h6>
                                        <% if (item.description) { %>
                                        <small class="text-muted"><%= item.description %></small>
                                        <% } %>
                                    </div>
                                    <div class="col-md-3 text-end">
                                        <span class="price">$<%= item.price.toFixed(2) %></span>
                                        <% if (config.whatsapp) { %>
                                        <a href="https://wa.me/<%= config.whatsapp.replace(/[^0-9]/g, '') %>?text=Hola, quiero pedir: <%= item.name %> - $<%= item.price.toFixed(2) %>" 
                                           class="btn btn-sm btn-success ms-2" target="_blank">
                                            <i class="fab fa-whatsapp"></i>
                                        </a>
                                        <% } %>
                                    </div>
                                </div>
                            </div>
                            <% }); %>
                        </div>
                    </div>
                </div>
            </div>
            <% }); %>
        </div>
    </section>

    <!-- Contact Section -->
    <section id="contact" class="py-5">
        <div class="container">
            <div class="text-center mb-5">
                <h2 class="display-5 fw-bold">Contacto</h2>
                <p class="lead">¿Tienes alguna pregunta? ¡Contáctanos!</p>
            </div>
            <div class="row">
                <div class="col-lg-6 mb-4">
                    <div class="card h-100">
                        <div class="card-header bg-primary text-white">
                            <h5 class="mb-0">Envíanos un mensaje</h5>
                        </div>
                        <div class="card-body">
                            <form onsubmit="submitContactForm(event)">
                                <div class="mb-3">
                                    <label for="name" class="form-label">Nombre</label>
                                    <input type="text" class="form-control" id="name" name="name" required>
                                </div>
                                <div class="mb-3">
                                    <label for="email" class="form-label">Email</label>
                                    <input type="email" class="form-control" id="email" name="email" required>
                                </div>
                                <div class="mb-3">
                                    <label for="phone" class="form-label">Teléfono</label>
                                    <input type="tel" class="form-control" id="phone" name="phone">
                                </div>
                                <div class="mb-3">
                                    <label for="message" class="form-label">Mensaje</label>
                                    <textarea class="form-control" id="message" name="message" rows="4" required></textarea>
                                </div>
                                <button type="submit" class="btn btn-primary">Enviar mensaje</button>
                            </form>
                        </div>
                    </div>
                </div>
                <div class="col-lg-6">
                    <div class="card h-100">
                        <div class="card-header bg-secondary text-white">
                            <h5 class="mb-0">Información de contacto</h5>
                        </div>
                        <div class="card-body">
                            <div class="mb-4">
                                <h6><i class="fas fa-map-marker-alt text-primary me-2"></i>Dirección</h6>
                                <p class="text-muted"><%= config.address || 'Dirección no disponible' %></p>
                            </div>
                            <div class="mb-4">
                                <h6><i class="fas fa-phone text-primary me-2"></i>Teléfono</h6>
                                <p class="text-muted"><%= config.phone || 'Teléfono no disponible' %></p>
                            </div>
                            <div class="mb-4">
                                <h6><i class="fas fa-envelope text-primary me-2"></i>Email</h6>
                                <p class="text-muted"><%= config.email || 'Email no disponible' %></p>
                            </div>
                            <div class="mb-4">
                                <h6><i class="fas fa-clock text-primary me-2"></i>Horarios</h6>
                                <p class="text-muted"><%- config.hours || 'Horarios no disponibles' %></p>
                            </div>
                            
                            <div class="d-grid gap-2">
                                <% if (config.whatsapp) { %>
                                <a href="https://wa.me/<%= config.whatsapp.replace(/[^0-9]/g, '') %>?text=Hola, quiero hacer una reserva" 
                                   class="btn btn-success" target="_blank">
                                    <i class="fab fa-whatsapp me-2"></i>Reservar por WhatsApp
                                </a>
                                <% } %>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- WhatsApp Float Button -->
    <% if (config.whatsapp) { %>
    <a href="https://wa.me/<%= config.whatsapp.replace(/[^0-9]/g, '') %>?text=Hola, me interesa conocer más sobre La Quinta Snack Bar" 
       class="whatsapp-float" target="_blank" title="Contáctanos por WhatsApp">
        <i class="fab fa-whatsapp"></i>
    </a>
    <% } %>

    <!-- Footer -->
    <footer>
        <div class="container">
            <div class="row">
                <div class="col-md-4">
                    <h5><i class="fas fa-utensils"></i> <%= config.name || 'La Quinta Snack Bar' %></h5>
                    <p>Donde cada bocado es una experiencia única.</p>
                </div>
                <div class="col-md-4">
                    <h5>Contacto</h5>
                    <p><i class="fas fa-phone"></i> <%= config.phone || '' %></p>
                    <p><i class="fas fa-envelope"></i> <%= config.email || '' %></p>
                    <p><i class="fas fa-map-marker-alt"></i> <%= config.address || '' %></p>
                </div>
                <div class="col-md-4">
                    <h5>Síguenos</h5>
                    <div class="social-links">
                        <% if (config.facebook) { %>
                        <a href="<%= config.facebook %>" target="_blank"><i class="fab fa-facebook"></i></a>
                        <% } %>
                        <% if (config.instagram) { %>
                        <a href="<%= config.instagram %>" target="_blank"><i class="fab fa-instagram"></i></a>
                        <% } %>
                        <% if (config.twitter) { %>
                        <a href="<%= config.twitter %>" target="_blank"><i class="fab fa-twitter"></i></a>
                        <% } %>
                        <% if (config.whatsapp) { %>
                        <a href="https://wa.me/<%= config.whatsapp.replace(/[^0-9]/g, '') %>" target="_blank"><i class="fab fa-whatsapp"></i></a>
                        <% } %>
                    </div>
                </div>
            </div>
            <hr class="my-4">
            <div class="text-center">
                <p>&copy; 2024 <%= config.name || 'La Quinta Snack Bar' %>. Todos los derechos reservados.</p>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Smooth scrolling
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Contact form
        function submitContactForm(event) {
            event.preventDefault();
            const form = event.target;
            const formData = new FormData(form);
            
            fetch('/contact', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/x-www-form-urlencoded',
                },
                body: new URLSearchParams(formData)
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    alert('¡Mensaje enviado correctamente! Te contactaremos pronto.');
                    form.reset();
                } else {
                    alert('Error al enviar el mensaje. Inténtalo de nuevo.');
                }
            })
            .catch(error => {
                alert('Error al enviar el mensaje. Inténtalo de nuevo.');
            });
        }
    </script>
</body>
</html>